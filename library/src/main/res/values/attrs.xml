<?xml version="1.0" encoding="utf-8"?>
<resources>


    <!-- ————————————————————————————————————————— library-ultra-pull-to-refresh———————————————————————————————————————— -->
    <declare-styleable name="LoadingViewFinal">
        <attr name="loadMoreMode">
            <flag name="click" value="0x0"/>
            <flag name="scroll" value="0x1"/>
        </attr>
        <attr name="loadMoreView" format="string"/>
        <attr name="noLoadMoreHideView" format="boolean"/>
    </declare-styleable>

    <declare-styleable name="PtrFrameLayout">
        <!-- Optional. If you put header and content in xml, you can you these to specify them. -->
        <attr name="ptr_header" format="reference"/>
        <attr name="ptr_content" format="reference"/>
        <attr name="ptr_resistance" format="float"/>  <!-- the resistance when you are moving the frame -->
        <attr name="ptr_ratio_of_header_height_to_refresh" format="float"/><!-- the ration of the height of the header to trigger refresh -->
        <attr name="ptr_duration_to_close" format="integer"/>
        <attr name="ptr_duration_to_close_header" format="integer"/>
        <!-- pull to refresh, otherwise release to refresh, default is release to refresh -->
        <attr name="ptr_pull_to_fresh" format="boolean"/>
        <!-- keep header when refreshing-->
        <attr name="ptr_keep_header_when_refresh" format="boolean"/>
    </declare-styleable>

    <declare-styleable name="PtrClassicHeader">
        <attr name="ptr_rotate_ani_time" format="integer"/>
    </declare-styleable>

    <!-- ————————————————————————————————————————— galleryfinal ———————————————————————————————————————— -->

    <declare-styleable name="GFFloatingActionButton">
        <attr name="fabColorPressed" format="color"/>
        <attr name="fabColorNormal" format="color"/>
        <attr name="fabIcon" format="reference"/>
        <attr name="fabTitle" format="string"/>
    </declare-styleable>

    <declare-styleable name="HorizontalListView">
        <attr name="dividerWidth" format="dimension"/>
        <attr name="android:divider"/>
        <attr name="android:requiresFadingEdge"/>
        <attr name="android:fadingEdgeLength"/>
    </declare-styleable>
</resources>